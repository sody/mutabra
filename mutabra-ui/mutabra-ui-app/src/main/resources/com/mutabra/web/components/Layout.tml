<!DOCTYPE html>
<html xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd"
	  xmlns:p="tapestry:parameter">
<head>
	<title>${title}</title>
	<meta content="no-cache" http-equiv="pragma"/>
	<meta content="no-store" http-equiv="cache-control"/>
	<meta content="text/html; charset=utf-8;" http-equiv="content-type"/>
</head>
<body>

<div class="container">
	<div class="unit header">
		<a href="/" class="logo">UTABRA</a>
		<div style="float: right;">
			<t:if test="${loggedIn}">
				<table class="dialog">
					<tr>
						<td>${message:welcome}</td>
					</tr>
					<tr>
						<td><span style="font-weight: bold;">${user}</span></td>
						<td>
							<a href="#">${message:button.sign-out.title}</a>
						</td>
					</tr>
				</table>
				<p:else>
					<a href="#">${message:button.sign-in.title}</a>
				</p:else>
			</t:if>
		</div>
	</div>

	<div class="unit c-5">
		<div class="column">
			<div class="container left">
				<ul class="m-menu">
					<li>
						<t:pagelink page="index">${message:page.index.title}</t:pagelink>
					</li>
					<li>
						<t:pagelink page="admin/roles">${message:page.admin-roles.title}</t:pagelink>
					</li>
				</ul>

				<t:menu items="index,roles">
					<p:indexMenuItem>
						<t:pagelink page="index">${message:page.index.title}</t:pagelink>
					</p:indexMenuItem>
					<p:rolesMenuItem>
						<t:pagelink page="admin/roles">${message:page.admin-roles.title}</t:pagelink>
					</p:rolesMenuItem>
				</t:menu>
			</div>
		</div>

		<div class="column s-3">
			<t:panel title="prop:title" class="center">
				<t:body/>
			</t:panel>
		</div>

		<div class="column">
			<div class="container right">
				<t:delegate to="rightBody"/>
			</div>
		</div>

	</div>

	<div class="unit footer">
		Copyright © 2010 Ivan Khalopik. All rights reserved.
		<br/>
		<a href="mailto:ikhalopik@gmail.com">Contact Us</a>
	</div>
</div>

</body>
</html>
