<?xml version="1.0"?>
<!DOCTYPE Configure PUBLIC "-//Mort Bay Consulting//DTD Configure//EN" "http://jetty.mortbay.org/configure.dtd">
<Configure id="ga" class="org.mortbay.jetty.webapp.WebAppContext">

  <New id="jdbc/mutabra" class="org.mortbay.jetty.plus.naming.Resource">
    <Arg>jdbc/mutabra</Arg>
    <Arg>
      <New class="org.apache.commons.dbcp.BasicDataSource">
        <Set name="driverClassName">${project.db.driver}</Set>
        <Set name="url">${project.db.url}</Set>
        <Set name="username">${project.db.username}</Set>
        <Set name="password">${project.db.password}</Set>
        <Set name="maxActive">10</Set>
        <Set name="maxIdle">3</Set>
        <Set name="maxWait">10000</Set>

        <Call name="addConnectionProperty">
          <Arg>useUnicode</Arg>
          <Arg>true</Arg>
        </Call>

        <Call name="addConnectionProperty">
          <Arg>characterEncoding</Arg>
          <Arg>UTF-8</Arg>
        </Call>
      </New>
    </Arg>
  </New>

  <New id="mail" class="org.mortbay.jetty.plus.naming.Resource">
    <Arg>mail/session</Arg>
    <Arg>
      <New class="org.mortbay.naming.factories.MailSessionReference">
        <Set name="user">${project.mail.admin-address}</Set>
        <Set name="password">${project.mail.admin-password}</Set>
        <Set name="properties">
          <New class="java.util.Properties">
            <Put name="mail.smtp.host">smtp.gmail.com</Put>
            <Put name="mail.smtp.port">587</Put>
            <Put name="mail.smtp.auth">true</Put>
            <Put name="mail.smtp.starttls.enable">true</Put>
            <Put name="mail.debug">true</Put>
          </New>
        </Set>
      </New>
    </Arg>
  </New>
</Configure>
