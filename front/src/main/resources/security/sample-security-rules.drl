package drools.sample

import org.greatage.security.acl.drools.DroolsAccessControlEntry;

rule "adminRule"
when
	$ace : DroolsAccessControlEntry(granted == false, authority == "admin@example.com")
then
	$ace.grant();
end